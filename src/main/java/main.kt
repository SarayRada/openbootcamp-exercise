fun main() {
    suma(3, 4 ,5)
    val myCar = Car()
    myCar.addDoors(5)
    myCar.seeDoors()
}

fun suma (`first-number`: Int, secondNumber: Int, thirdNumber: Int): Int {
    return `first-number` + secondNumber + thirdNumber
}